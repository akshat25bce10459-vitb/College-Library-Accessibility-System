# College-Library-Accessibility-System
This Python script uses a list of dictionaries to mimic a library inventory system.  The **Librarian** (using `update_book_availability` to modify stock levels and set return dates) and the **Student** (using `check_book_status` for case-insensitive title search and viewing availability) share the core functionality.
